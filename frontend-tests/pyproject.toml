[project]
name = "alpha-app-frontend-tests"
version = "0.1.0"
description = "Frontend isolation tests for Alpha-App"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "playwright>=1.40",
]

[tool.pytest.ini_options]
testpaths = ["."]
